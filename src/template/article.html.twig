{% extends 'header.html.twig' %}

{% block content %}

    <body>
        <div class="container">
            <div class="row">
                <div class="col .green">
                    <h2>{{ article.title }}</h2>
                    <p><small>{{ article.creationDate }}</small></p>
                    <p>{{ article.content }}</p>
                </div>
            </div>
        </div>
    </body>

{% endblock %}